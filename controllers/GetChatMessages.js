const socketService=require('../services/OnlineUsersServices');let getChatMessages=(req,res)=>{    let messageDetails=req.body.getMessageIdentity;    let messageID1=`${messageDetails.senderID}${messageDetails.receiverID}`;    let messageID2=`${messageDetails.receiverID}${messageDetails.senderID}`;    socketService.getMessages(messageID1,messageID2).then(rows=>{        res.send(rows);    })}module.exports=getChatMessages;